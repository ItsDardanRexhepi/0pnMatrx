{
  "name": "0pnMatrx",
  "install": "if [ -f .cursor/install.sh ]; then bash .cursor/install.sh; else bash repos/0pnMatrx/.cursor/install.sh; fi",
  "terminals": [
    {
      "name": "gateway",
      "command": "cd \"$([ -d .venv ] && echo . || echo repos/0pnMatrx)\" && source .venv/bin/activate && python -m gateway.server",
      "description": "0pnMatrx gateway (REST + WebSocket) on http://localhost:18790 — /health, /status, /chat, /api/v1/capabilities"
    }
  ],
  "ports": [
    {
      "name": "gateway",
      "port": 18790
    }
  ]
}
